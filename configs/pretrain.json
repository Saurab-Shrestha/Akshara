{
  "_comment": "Stage-specific overrides for language-only pretraining. Model architecture comes from base.json.",
  "train_path": "data/corpus/train.jsonl",
  "dev_path": "data/corpus/val.jsonl",
  "batch_size": 16,
  "grad_accum": 4,
  "train_steps": 50000,
  "eval_steps": 500,
  "warmup_steps": 2000,
  "lr": 3e-4,
  "min_lr": 3e-5,
  "out_ckpt": "checkpoints/pretrain.pt",
  "log_dir": "logs/pretrain"
}
